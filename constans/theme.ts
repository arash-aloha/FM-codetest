const COLORS = {
  primaryDark: "#171515",
  primaryGray: "#E5E5E5",
  primaryWhite: "#FFF",
  primaryGreen: "#3CBFAE",
};

const FONT_FAM = {
  WS_regular: "WorkSans-Regular",
  WS_bold: "WorkSans-Bold",
};

const FONT = {
  regular: "300" as "300", //TS workaround
  medium: "700" as "700",
  bold: "bold" as "bold",
};

const PADDING = {
  small: 20,
  medium: 30,
};

const MARGIN = {
  small: 20,
  medium: 30,
};

const SIZES = {
  xSmall: 8,
  small: 12,
  medium: 16,
  large: 24,
  xLarge: 40,
  xxLarge: 48,
};

export { COLORS, FONT, SIZES, PADDING, MARGIN, FONT_FAM };
